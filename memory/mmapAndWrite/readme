"mmapAndWrite" program is allocated 100MB by mmap() system call each time, and the new memory area is wholly writed once by zero.

VmSize :  virtual memory size
VmRSS  :  roughly physical memory size

They can be seen from /proc/pid/status.
