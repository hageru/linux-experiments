"mmap" program is allocated 200MB by mmap() system call and afterwards released 100MB by munmap() system call every time.

VmSize :  virtual memory size
VmRSS  :  roughly physical memory size

They can be seen from /proc/pid/status.
